import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'time-angular-v4',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
